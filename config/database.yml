# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000

development:
  <<: *default
  adapter: postgresql
  #database: db/development.sqlite3
  database: explore-so-dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  adapter: postgresql
  #database: db/test.sqlite3
  database:  explore-so-test

#production:
 # <<: *default
  #database: db/production.sqlite3
  


production:
  #<<: *default
  #database: db/production.sqlite3
  adapter: postgresql
  host: localhost
  database: explore-so
  username: 
  password: 
  pool: 5
  timeout: 5000