<h2>Course Catalogs by Semester</h2>
<p>Looking to take a course? SUNY has thousands of courses available every semester. 
  The options below will allow you to search for courses available in whatever semester you need. 
  Click a term below to get started!
</p>
<table class="table table-striped table-hover table-bordered">
  <% @catalogs.each_slice(2) do |row| %>
  <tr>
     <% row.each do |catalog| %>
    <td class="catalog" width="50%"><a href="/courses?filterrific[with_catalog_id]=<%=catalog.id %>"><%= catalog.name %></td>
    <%  end %>
    </tr>
    <%  end %>
</table>